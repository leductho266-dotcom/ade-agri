# Streamlit Cloud đọc file này. GIỮ NHẸ.
# Không thêm tensorflow / pmdarima / statsmodels vào đây — app chỉ ĐỌC từ CSDL,
# không train gì cả. Thêm vào sẽ làm build chậm và tràn RAM free tier (~1GB).
streamlit>=1.36
sqlalchemy>=2.0
psycopg[binary]>=3.1
pandas>=2.0
plotly>=5.20
anthropic>=0.34
